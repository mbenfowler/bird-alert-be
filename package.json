{
  "name": "bird-alert",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "knex": "^3.0.1",
    "pg": "^8.11.3"
  },
  "scripts": {
    "migrate:latest": "knex migrate:latest",
    "seed:run": "knex seed:run",
    "vercel-build": "npm run migrate:latest && npm run seed:run"
  }
}
